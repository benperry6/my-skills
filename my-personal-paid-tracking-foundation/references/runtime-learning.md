# Runtime Learning — my-personal-paid-tracking-foundation

Structured runtime incidents should be mirrored here for human review.

Use `runtime-learning.json` as the machine-readable source of truth when runtime incidents are captured through the shared verified-learning loop.

No runtime incidents have been recorded yet.
